# pnpm settings (pnpm 11 moved these out of package.json).
# Allow native install scripts we trust: sharp (Next.js image optimization) and
# unrs-resolver (ESLint's native module resolver).
allowBuilds:
    sharp: true
    unrs-resolver: true
onlyBuiltDependencies:
    - sharp
    - unrs-resolver
