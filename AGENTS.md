# Agents

Read `CLAUDE.md` first: it is the project instruction file, and this file only points at it.
Then `CONVENTIONS.md` for the rules with an ID, and `STATE.md` for where the work stands.
Run `./scripts/verify.sh` before calling a change done.
